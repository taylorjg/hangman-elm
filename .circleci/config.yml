version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7.10
    working_directory: ~/repo
    steps:
      - checkout
      - run: npm install create-elm-app
      - run: elm-app build
      # - run: elm-app test
