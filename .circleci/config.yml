version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7.10
    working_directory: ~/repo
    steps:
      - checkout
      - run: sudo yarn global add create-elm-app
      - run: elm-app build
      - run: elm-app test
